version: '3.8'

services:
  db:
    image: postgres:15
    container_name: anipals_db
    restart: always
    environment:
      POSTGRES_DB: anipals_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: yourpassword
    ports:
      - "5432:5432"